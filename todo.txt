opcodes?

00E0 clear screen
00EE return from subroutine

1NNN jump
2NNN execute NNN
3XNN skip if VX == NN
4XNN skip if VX != NN 
5XY0 skip if vx == vy
6XNN store nn in vx
7XNN add nn to vx
8XY0 store vy in vx
8XY1 set vx to vx | vy
8XY2 set vx to vx & vy
8XY3 set vx to vx ^ vy
8XY4 add vy to vx. set vf to 01 if carry occurs, 00 else
8XY5 subtract vy from vx, vf to 00 if a borrow occurs
8XY6 


display sprites